import cv2
import numpy as np
import torch
import os 



def polar_to_mat(theta,phi,r):
	""" use r, theta phi to generate transformation matrix for generating pose of camera w.r.t world """
	pass
def poses_to_render(theta_range, phi_range, r_range):
	""" generates poses along a range of radii, azimuth and altitude values for rendering image """
	pass
def render_nerf():
	pass